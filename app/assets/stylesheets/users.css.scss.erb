#account {
  header {
    padding-bottom: 2%;
  }
  
  label {
    display: inline-block;
    width: 155px;
  }
  
  input[type=text], input[type=password], input[type=email], select {
    width: 300px;
  }
  
  .field_with_errors {
    display: inline;
  }
}

#error_explanation {
  color: #fc3f4d;
}